#TODO
# 1) Detect compiler
# 2) Propose option to choose compiler ( set by defaut to the detected compiler )
# 3) Setup CMake build to use the already existing build script for this compiler if it exists.
# 4) If it doesnt exists, fail. ( or create it ? )
